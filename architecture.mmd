%% gapsi-ecommerce — capas y UX

flowchart TB
  subgraph app["app/ (Next)"]
    R["rutas / metadata / manifest PWA"]
    API["Route Handlers"]
  end

  subgraph features["src/features/* (dominio)"]
    subgraph products["products"]
      PUI["ui molecules → organisms → templates"]
      PH["hooks + React Query"]
      PS["server gateways"]
    end
    subgraph cart["cart"]
      CUI["sidebar / items"]
      Z["Zustand store"]
    end
  end

  subgraph shared["src/shared"]
    A["ui atoms + layouts"]
    U["utils / constants / SEO"]
  end

  app --> features
  features --> shared
  R --> products
  PH --> API
